{
  "private": true,
  "name": "cf-access-basic-authenticator",
  "version": "1.0.0",
  "description": "Cloudflare worker that allows you to pass Basic Auth instead of headers to CF Access apps",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "format": "prettier --write '**/*.{js,css,json,md}'"
  },
  "author": "Bojan Zelic",
  "license": "MIT",
  "devDependencies": {
    "prettier": "^1.18.2"
  }
}
